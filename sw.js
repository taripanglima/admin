self.addEventListener('push',event=>{let data={title:'Roda Impian PGPM',body:'Permohonan admin baharu.',url:'/'};try{data={...data,...event.data.json()}}catch(e){}event.waitUntil(self.registration.showNotification(data.title,{body:data.body,icon:'/assets/pgpm.png',badge:'/assets/pgpm.png',data:{url:data.url||'/'},tag:data.tag||'pgpm-admin',renotify:true}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(cs=>{for(const c of cs){if('focus'in c){c.navigate(url);return c.focus()}}return clients.openWindow(url)}))});
