import webpush from 'npm:web-push@3.6.7';
const SUPABASE_URL=Deno.env.get('SUPABASE_URL')!; const SERVICE=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;
const VAPID_PUBLIC_KEY=Deno.env.get('VAPID_PUBLIC_KEY')!; const VAPID_PRIVATE_KEY=Deno.env.get('VAPID_PRIVATE_KEY')!; const VAPID_SUBJECT=Deno.env.get('VAPID_SUBJECT')!;
webpush.setVapidDetails(VAPID_SUBJECT,VAPID_PUBLIC_KEY,VAPID_PRIVATE_KEY);
async function rpc(name:string,body:Record<string,unknown>={}){const r=await fetch(`${SUPABASE_URL}/rest/v1/rpc/${name}`,{method:'POST',headers:{apikey:SERVICE,Authorization:`Bearer ${SERVICE}`,'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw new Error(await r.text());return await r.json()}
Deno.serve(async req=>{try{const payload=await req.json();const record=payload.record||payload;if(!record?.recipient_user_id)return new Response(JSON.stringify({ignored:true}),{headers:{'Content-Type':'application/json'}});const targets=await fetch(`${SUPABASE_URL}/rest/v1/ripgpm_push_subscriptions?user_id=eq.${record.recipient_user_id}&select=endpoint,p256dh,auth`,{headers:{apikey:SERVICE,Authorization:`Bearer ${SERVICE}`}});const subs=targets.ok?await targets.json():[];const data={title:record.title||'Roda Impian PGPM',body:record.body||'Notifikasi baharu',url:'https://adminpgpm.pages.dev/',tag:`pgpm-${record.type||'notification'}-${record.id}`};const results=[];for(const s of subs){try{await webpush.sendNotification({endpoint:s.endpoint,keys:{p256dh:s.p256dh,auth:s.auth}},JSON.stringify(data));results.push({ok:true})}catch(e){results.push({ok:false})}}return new Response(JSON.stringify({ok:true,results}),{headers:{'Content-Type':'application/json'}})}catch(e){return new Response(JSON.stringify({error:String(e)}),{status:500,headers:{'Content-Type':'application/json'}})}});
